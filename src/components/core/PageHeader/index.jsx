/**
 * Navix Core — PageHeader
 * --------------------------------------------------------------------------
 * Ré-export du composant générique existant (source unique de vérité dans
 * src/components/layout/PageHeader). Documenté dans la bibliothèque core.
 */
export { default } from '../../layout/PageHeader';
