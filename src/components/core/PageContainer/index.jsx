/**
 * Navix Core — PageContainer
 * --------------------------------------------------------------------------
 * Ré-export du composant générique existant (source unique de vérité dans
 * src/components/layout/PageContainer). Documenté dans la bibliothèque core.
 */
export { default } from '../../layout/PageContainer';
