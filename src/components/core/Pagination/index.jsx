/**
 * Navix Core — Pagination
 * --------------------------------------------------------------------------
 * Ré-export de la pagination générique existante (source unique de vérité
 * dans src/components/ui/Pagination). Tous les modules doivent passer par
 * cette entrée de la bibliothèque core.
 */
export { default } from '../../ui/Pagination';
