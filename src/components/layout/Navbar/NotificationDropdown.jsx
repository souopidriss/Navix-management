/**
 * Navix — NotificationDropdown (Header global)
 * --------------------------------------------------------------------------
 * Pont entre le Navbar existant et le centre de notifications du module
 * `@/features/notifications` : cloche avec badge de non-lues, dernières
 * notifications et lien vers la page Notifications.
 */
export { default } from '@/features/notifications/components/NotificationDropdown';
