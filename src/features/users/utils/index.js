/**
 * Navix Users — Utilitaires du module.
 */
export {
  resolveRolePermissions,
  getPermissionsByRoleIds,
  getEffectivePermissions,
  getUserRoles,
  getUserRoleCodes,
  hasRole,
  hasAnyRole,
  hasPermission,
  hasAnyPermission,
  hasAllPermissions,
  canAccessModule,
} from './access';
