/**
 * Navix Driver Dashboard — PlaceholderPage
 * --------------------------------------------------------------------------
 */
const PlaceholderPage = ({ title }) => (
  <div className="p-4">
    <h4>{title}</h4>
    <p className="text-muted">Page en cours de construction.</p>
  </div>
);

export default PlaceholderPage;
